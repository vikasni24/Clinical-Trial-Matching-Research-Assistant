/** Literal hex values mirroring src/index.css's custom properties — kept
 * here because chart libraries generally need literal color values rather
 * than CSS custom properties for canvas/SVG fills. Keep these in sync with
 * index.css's :root palette. */
export const chartColors = {
  blue: "#3b82f6",
  blueBright: "#5b9dff",
  green: "#2ee6a6",
  red: "#ff5470",
  amber: "#ffb454",
  gray: "#8b95ab",
  grid: "#1c2333",
  textMuted: "#97a1b8",
  surface: "#121826",
};
